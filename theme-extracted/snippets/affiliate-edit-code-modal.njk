<div
  x-show="appAffiliate.modalEditCodeIsOpen"
  x-cloak
  class="fixed inset-0 z-50 overflow-y-auto"
  aria-labelledby="affiliate-edit-code-modal-title"
  role="dialog"
  aria-modal="true"
>
  <div class="flex items-center justify-center min-h-screen px-4 text-center">
    <div
      x-show="appAffiliate.modalEditCodeIsOpen"
      x-transition:enter="transition ease-out duration-300 transform"
      x-transition:enter-start="opacity-0"
      x-transition:enter-end="opacity-100"
      x-transition:leave="transition ease-in duration-200 transform"
      x-transition:leave-start="opacity-100"
      x-transition:leave-end="opacity-0"
      @click="appAffiliate.modalEditCodeClose()"
      class="fixed inset-0 transition-opacity lux-modal-backdrop"
      aria-hidden="true"
    ></div>

    <div
      x-show="appAffiliate.modalEditCodeIsOpen"
      x-transition:enter="transition ease-out duration-300 transform"
      x-transition:enter-start="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
      x-transition:enter-end="opacity-100 translate-y-0 sm:scale-100"
      x-transition:leave="transition ease-in duration-200 transform"
      x-transition:leave-start="opacity-100 translate-y-0 sm:scale-100"
      x-transition:leave-end="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
      class="inline-block w-full max-w-lg p-6 my-8 overflow-hidden text-left align-middle transition-all transform glass rounded-lux-lg text-t-primary"
    >
      <div class="flex items-start justify-between gap-4 mb-4">
        <h3 id="affiliate-edit-code-modal-title" class="text-xl font-bold truncate">
          Edit Affiliate Code
        </h3>

        <button @click="appAffiliate.modalEditCodeClose()" class="text-t-primary focus:outline-none">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-7 text-t-primary/50 hover:text-lux-blue-light transition-colors duration-200">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>

      <form @submit.prevent="appAffiliate.submitEditCode()">
        <div class="mb-4">
          <label class="block text-t-primary/80 mb-1">
            Code
          </label>
          <input
            type="text"
            x-model="appAffiliate.editCodeValue"
            x-ref="appAffiliate.editCodeInput"
            placeholder="Enter your affiliate code"
            maxlength="50"
            class="lux-input"
            required
          />
        </div>

        <p x-show="appAffiliate.editCodeError" class="text-lux-error text-sm mt-2" x-text="appAffiliate.editCodeError"></p>

        <button
          type="submit"
          class="btn-primary w-full mt-4 text-base"
          :disabled="appAffiliate.editCodeLoading"
        >
          <span>Update</span>
          <template x-if="!appAffiliate.editCodeLoading">
            <i class="fa fa-arrow-right"></i>
          </template>
          <template x-if="appAffiliate.editCodeLoading">
            <i class="fa fa-circle-notch animate-spin"></i>
          </template>
        </button>
      </form>
    </div>
  </div>
</div>